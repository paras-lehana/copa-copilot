# Temporary JSON placeholder for copa-copilot-api (replaced by the real Express API on D2).
# Kept in infra/placeholder/ — swept from the submission tree before final push (S49-T06).
FROM nginx:alpine
COPY default.conf /etc/nginx/conf.d/default.conf
