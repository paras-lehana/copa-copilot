server {
  listen 80;
  server_name _;
  charset utf-8;

  location = /api/meta {
    default_type application/json;
    return 200 '{"service":"copa-copilot-api","version":"0.0.1-placeholder","status":"live","message":"Placeholder - the real API (crowd, routing, egress, weather, assistant) arrives with the first full deploy.","project":"PromptWars Challenge 4 - Smart Stadiums & Tournament Operations"}';
  }

  location / {
    default_type application/json;
    return 200 '{"service":"copa-copilot-api","status":"live","hint":"try GET /api/meta"}';
  }
}
