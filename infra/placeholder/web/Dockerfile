# Temporary branded placeholder for copa-copilot-web (replaced by the real Next.js app on D2).
# Kept in infra/placeholder/ — swept from the submission tree before final push (S49-T06).
FROM nginx:alpine
COPY index.html /usr/share/nginx/html/index.html
